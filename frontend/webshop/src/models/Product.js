export default class Product {
    constructor(id, name, price, amount, description, picture) {
      this.id = id;
      this.name = name;
      this.price = price;
      this.amount = amount;
      this.description = description;
      this.picture = picture;
    }
  }