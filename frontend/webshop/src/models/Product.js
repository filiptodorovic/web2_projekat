export default class Product {
    constructor(id, name, price, amount, description, picture,pictureUrl) {
      this.id = id;
      this.name = name;
      this.price = price;
      this.amount = amount;
      this.description = description;
      this.picture = picture;
      this.pictureUrl=pictureUrl;
    }
  }